# Frontend env — Vite reads variables prefixed with VITE_
# Leave VITE_API_URL blank for localhost dev (uses Vite proxy at port 5000).
# On Render (static site), set this to your backend Render service URL.
VITE_API_URL=https://zenmind-backend.onrender.com
