# dependencies
node_modules

# env files — ignore real secrets, but commit the examples
.env
.env.local
.env.*.local
backend/.env
backend/.env.local
!.env.example
!backend/.env.example

# build outputs (dist is NOT gitignored — Render needs it for static site publishing)
# dist

# logs
*.log

